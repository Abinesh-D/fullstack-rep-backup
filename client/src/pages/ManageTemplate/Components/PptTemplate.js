import React from "react";

const PptTemplate = ({ title, thumbnailUrl, pptUrl, onShare }) => {


    return (
        <div className="ppt-template-card">
       HEllo
        </div>
    );
};

export default PptTemplate;
